# ===========================================
# FourthChat Environment Configuration
# ===========================================
# Copy this file to .env.local and fill in the values

# ------------------------------------------
# Database (Required)
# ------------------------------------------
# PostgreSQL connection string
POSTGRES_URL="postgresql://postgres:postgres@localhost:5432/fourthchat"

# ------------------------------------------
# Authentication (Required)
# ------------------------------------------
# Secret key for signing sessions (generate with: openssl rand -base64 32)
AUTH_SECRET=""

# ------------------------------------------
# Vector Database (Required)
# ------------------------------------------
# Qdrant vector database URL
QDRANT_URL="http://localhost:6333"

# ------------------------------------------
# AI Provider API Keys (Optional - can be configured in dashboard)
# ------------------------------------------
# OpenAI API Key (for GPT models)
# OPENAI_API_KEY=""

# Google Generative AI API Key (for Gemini models)
# GOOGLE_GENERATIVE_AI_API_KEY=""

# ------------------------------------------
# Embeddings Configuration (Optional)
# ------------------------------------------
# Embedding provider: "openai" or "google" (defaults to openai)
# EMBEDDING_PROVIDER="openai"
